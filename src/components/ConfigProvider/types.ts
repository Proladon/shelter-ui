export interface ThemeVarsConfig {
  primary: string
  'primary-light': string
  bg: {
    primary: string
    secondary: string
  }

  text: {
    base: string
  }

  border: {
    base: string
  }

  status: {
    info: string
    danger: string
    warning: string
    success: string
  }
}
