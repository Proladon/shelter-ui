export interface ThemeVarsConfig {
  primary: string
  bg: {
    primary: string
    secondary: string
  }

  text: {
    base: string
  }

  border: {
    base: string
  }

  status: {
    info: string
    danger: string
    warning: string
    success: string
  }
}
