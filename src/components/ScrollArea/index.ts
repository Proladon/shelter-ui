import ScrollArea from './index.vue'
import type {
  ScrollAreaProps,
  ScrollAreaMethods,
  ScrollAreaSlots,
} from './types'

export { ScrollArea }
export type { ScrollAreaProps, ScrollAreaMethods, ScrollAreaSlots }
export default ScrollArea
